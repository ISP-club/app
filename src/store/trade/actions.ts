import { ActionTree } from 'vuex';
import { StateInterface } from '../index';
import { TradeStateInterface } from './state';

const actions: ActionTree<TradeStateInterface, StateInterface> = {
};

export default actions;
